// c code:


/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
int getDecimalValue(struct ListNode* head){
int ans=0;
while(head!='\0'){
ans*=2;
ans=ans+(head->val);
head=head->next;
}
return ans;
}
